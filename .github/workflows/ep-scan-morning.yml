name: EP Scan — Morning (BMO Earnings)

on:
  schedule:
    # Dual-cron for DST-aware 5:45 AM Pacific (Mon-Fri)
    # 12:45 PM UTC = 5:45 AM PDT (Mar-Nov)
    - cron: '45 12 * * 1-5'
    # 1:45 PM UTC = 5:45 AM PST (Nov-Mar)
    - cron: '45 13 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "ep-scan-morning"
  cancel-in-progress: false

jobs:
  ep-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Schedule guard
        id: schedule-guard
        env:
          WORKFLOW_EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_EVENT_SCHEDULE: ${{ github.event.schedule }}
          SCHEDULE_GUARD_PDT_CRON: "45 12 * * 1-5"
          SCHEDULE_GUARD_PST_CRON: "45 13 * * 1-5"
        run: |
          python3 -m src.ci.daily_screening_schedule_guard

      - name: Set up Python
        if: steps.schedule-guard.outputs.should_run == 'true'
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        if: steps.schedule-guard.outputs.should_run == 'true'
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run morning EP scan
        if: steps.schedule-guard.outputs.should_run == 'true'
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_API_SECRET: ${{ secrets.ALPACA_API_SECRET }}
          DISCORD_EP_SCAN_WEBHOOK_URL: ${{ secrets.DISCORD_EP_SCAN_WEBHOOK_URL }}
          PYTHONPATH: .
        run: |
          python src/reporting/ep_scan_morning.py || echo "EP scan failed - continuing"

      - name: Commit and push results
        if: steps.schedule-guard.outputs.should_run == 'true'
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add docs/data/ep_scan_morning.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "EP scan morning $(date +'%Y-%m-%d')"
            max_attempts=3
            attempt=1
            while [ "${attempt}" -le "${max_attempts}" ]; do
              git fetch origin "${BRANCH_NAME}"
              if ! git rebase "origin/${BRANCH_NAME}"; then
                git rebase --abort || true
                echo "::error::Rebase failed."
                exit 1
              fi
              if git push origin HEAD:${BRANCH_NAME}; then
                break
              fi
              if [ "${attempt}" -eq "${max_attempts}" ]; then
                echo "::error::Push failed after ${max_attempts} attempts."
                exit 1
              fi
              attempt=$((attempt + 1))
              sleep 5
            done
          fi

  deploy-pages:
    needs: ep-scan
    if: always() && needs.ep-scan.result == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: 'docs'
      - id: deployment
        uses: actions/deploy-pages@v4
